language: python
cache: pip

sudo: false

python:
  - "3.5"

install:
  - "python setup.py install"
  - "pip install codecov"

script:
  - "coverage run tests/test_utils.py"
  - "coverage run -a tests/test_parser.py"

after_success:
  - "codecov"
